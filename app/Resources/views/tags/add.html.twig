{% extends "base.html.twig" %}

{% block body %}

    <div class="container">
        <div class="col-md-4 col-md-offset-4">
            {{ form_start(tagForm) }}
            {{ form_widget(tagForm) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Add Tag</button>
            {{ form_end(tagForm) }}
        </div>
    </div>

{% endblock %}