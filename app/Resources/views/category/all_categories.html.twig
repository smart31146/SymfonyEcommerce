{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-md-4 col-md-offset-4">
            <ul>
                {% for category in categories %}
                    <h1>
                        <li><a href="{{ path('category_products', {'id': category.id}) }}"> {{ category }}</a></li>
                    </h1>
                {% endfor %}
            </ul>
        </div>
    </div>

{% endblock %}

